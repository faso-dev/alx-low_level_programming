#include <stdio.h>

/**
 * main - Entry point
 * prints "with proper grammar, but the outcome is a piece of art," with printf
 * Return: 0 for success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
